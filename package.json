{
  "name": "p3_31898868",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run backend\" \"cross-env REACT_APP_API_URL=http://localhost:3000/api PORT=3001 npm start --prefix frontend\"",
    "test": "cd backend && npm test && cd ../frontend && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0"
  }
}
